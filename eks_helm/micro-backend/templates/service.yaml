apiVersion: v1
kind: Service
metadata:
  name: micro-backend
spec:
  type: {{ .Values.service.type }}
  selector:
    app: micro-backend
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}